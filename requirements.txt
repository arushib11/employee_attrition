setuptools>=70.0.0,<82
pandas==2.1.4
numpy==1.26.4
scikit-learn==1.4.2
dvc==3.30.1
# DVC S3 support (pinned to avoid pip "resolution-too-deep" backtracking in CI)
dvc-s3==2.23.0
aiobotocore==2.21.1
boto3==1.37.1
botocore==1.37.1
s3transfer==0.11.0
jmespath==1.0.1
pathspec<0.12
mlflow==2.15.1
pytest==7.4.0
pytest-cov==5.0.0
pyyaml==6.0.1
evidently==0.4.3

# Constrain dependency resolver (prevents pip "resolution-too-deep" in CI)
# `evidently` requires a minimum FastAPI version. Newer FastAPI/Starlette releases require
# Python >=3.10, so we pin a 3.9 + 3.12 compatible stack.
fastapi==0.98.0
starlette==0.27.0
pydantic==1.10.26
